{
  "identifier": "triggerworkflow",
  "title": "TriggerWorkflow",
  "icon": "Github",
  "schema": {
    "properties": {
      "workflow_name": {
        "icon": "Github",
        "title": "Trigger playlist workflow",
        "type": "string",
        "description": "Self service to trigger playlist workflow"
      }
    },
    "required": ["self-service.yml"]
  }
}
